<p id="notice"><%= notice %></p>
<div>
  <div class="tab-content">
    <% @spots.each do |spot| %>
    <%= render 'box', spot: spot%>
    <% end %>
  </div>


    <div style="background-image: url(/images/bg.png); background-size: cover; background-position: 0% 85%;">
      <ul class="list nav nav-tabs start-tab" role="tablist">
        <% @spots.each do |spot| %>
        <%= render 'tabheader', spot: spot, index_id: "tab#{spot.id}", index_id_hash: "#" + "tab#{spot.id}"%>
        <% end %>
        </ul>
    </div>

</div>